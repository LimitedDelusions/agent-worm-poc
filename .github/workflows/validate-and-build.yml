name: Validate and Build Agent Worm POC
on:
  workflow_dispatch:
concurrency:
  group: agent-worm-poc-${{ github.ref }}
  cancel-in-progress: false
env:
  IMAGE_VERSION: 0.8.10
jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Verify immutable release ref
        shell: bash
        run: |
          test "$GITHUB_REF_TYPE" = "tag"
          test "$GITHUB_REF_NAME" = "v${IMAGE_VERSION}"
          test "$GITHUB_REF" = "refs/tags/v${IMAGE_VERSION}"
          test "$(tr -d '\r\n' < VERSION)" = "$IMAGE_VERSION"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install validation tools
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]" PyYAML==6.0.2
      - name: Normalize script permissions
        run: chmod +x scripts/runpod/*.sh
      - name: Compile and test
        env:
          PYTHONPATH: src
        run: |
          python -m compileall -q src scripts tests
          ruff check src scripts tests
          python -m pytest -q
      - name: Validate shell, JSON, and workflow YAML
        run: |
          find scripts -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n
          shellcheck scripts/runpod/*.sh
          python - <<'PYCODE'
          import json
          from pathlib import Path
          import yaml
          for path in Path('configs').glob('*.json'):
              json.loads(path.read_text(encoding='utf-8'))
          yaml.safe_load(Path('.github/workflows/validate-and-build.yml').read_text(encoding='utf-8'))
          print('JSON and YAML parsed successfully')
          PYCODE
      - name: Verify source integrity and release audit
        env:
          PYTHONPATH: src
        run: |
          python scripts/release/generate_integrity.py --check
          sha256sum -c SOURCE_HASHES.sha256
          python scripts/validate_release.py
      - name: Run complete simulated gated validation
        env:
          PYTHONPATH: src
        run: |
          rm -rf outputs/ci-fake-validation
          python scripts/run_gated.py fake-gated --root . --output-root outputs/ci-fake-validation
          python - <<'PYCODE'
          import json
          from pathlib import Path
          statuses=list(Path('outputs/ci-fake-validation').glob('*/RUN_STATUS.json'))
          assert len(statuses)==1,statuses
          value=json.loads(statuses[0].read_text())
          assert value['status']=='completed',value
          assert value['gates']['compatibility']['passed'],value
          assert value['gates']['positive_pair']['passed'],value
          assert value['gates']['shakedown']['passed'],value
          assert value['gates']['main']['analysis_valid'],value
          assert value['gates']['main']['question_feasibility_supported'],value
          assert value['evidence_status']=='verified',value
          run_dir=statuses[0].parent
          assert not (run_dir/'01_compatibility/decision_gates.json').exists()
          assert not (run_dir/'02_calibration/decision_gates.json').exists()
          assert (run_dir/'01_compatibility/compatibility_gate.json').is_file()
          assert (run_dir/'02_calibration/calibration_gates.json').is_file()
          assert (run_dir/'02_calibration/workflow_scores.csv').is_file()
          assert len((run_dir/'03_main/workflow_records.jsonl').read_text().splitlines())==672
          assert len((run_dir/'03_main/stage_events.jsonl').read_text().splitlines())==1344
          print(json.dumps(value,indent=2))
          PYCODE
          cp outputs/ci-fake-validation/*/RUN_STATUS.json outputs/ci-fake-validation/RUN_STATUS.json
          python scripts/release/verify_evidence.py \
            outputs/ci-fake-validation/agent-worm-results-*.zip --expected-version 0.8.10
      - uses: actions/upload-artifact@v4
        with:
          name: agent-worm-poc-validation
          path: |
            RELEASE_MANIFEST.json
            SOURCE_HASHES.sha256
            outputs/ci-fake-validation
          if-no-files-found: error
          retention-days: 30
  build:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Free disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af --volumes || true
          df -h
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v3
      - name: Compute image metadata
        id: image
        shell: bash
        run: |
          echo "name=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"
          echo "created=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"
      - name: Build and push image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          provenance: mode=max
          sbom: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            SOURCE_REPO=https://github.com/${{ github.repository }}
            GIT_REVISION=${{ github.sha }}
            BUILD_TIMESTAMP=${{ steps.image.outputs.created }}
          tags: |
            ${{ steps.image.outputs.name }}:${{ env.IMAGE_VERSION }}
            ${{ steps.image.outputs.name }}:sha-${{ github.sha }}
      - name: Write immutable RunPod image reference
        env:
          IMAGE_NAME: ${{ steps.image.outputs.name }}
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          mkdir -p dist
          printf '%s@%s\n' "$IMAGE_NAME" "$IMAGE_DIGEST" > dist/RUNPOD_IMAGE.txt
          cat dist/RUNPOD_IMAGE.txt
      - uses: actions/upload-artifact@v4
        with:
          name: agent-worm-poc-container-reference
          path: dist/RUNPOD_IMAGE.txt
          if-no-files-found: error
          retention-days: 90
